# 6장 연습문제

## 퍼즐

**가로**
4 멸개의 개체 타입들의 집합 - 카테고리
5 원래는 존재하지 e끓1지만 밀요에 따라 설계자가 추가한 속성 - 설계속성
8 두 개체 간의 M:N 관계로 인해 생성되는 개체 - 관계개체
10 독립적으로 존재하지 못하고 소유 개체가 있어야 존재할 수 있는 개체 - 약한개체
11 관계를 맺고 있는두 개체 타입에서 한 개체 타입의 일부 개체 인스턴스만 다른 개체 타입의 개체 인스턴스와 연관되는 것 - 부분참여

**세로**
1 한 개체가 다른 개체가 아닌 자기 자신과 맺는 관계 - 순환관계
2 업무로부터 도출된 일반적인 속성 - 기초속성
3 다른 속성으로부터 계산 등의 가공 처리를 하여 생성된 속성 - 유도속성
6 두 개체 간에 존재하는 2 개 이상의 관계 - 병렬관계
7 키 개체 간의 업무적 관련성으로 인해 생성되는 개체 - 메인개체
9 정보를 간단히 표현하기 위한 기호를 나타내는 개체 - 코드개체

## 연습문제

1. 디음 중 개체 심화 요소에 대한 설명으로 적절하지 않는 것은?   - 2 -
  1. 키 개체는 다른 개체의 부모 개체가 되므로 가장 먼저 추출해야 한다.
  2. 관계 개체는 ER 모텔에서 대개 관계에 해당한다.
  3. 코드 개체는 정보를 간단히 표현하기 위한 기호를 나타내는 개체다.
  4. 약한 개체는 소유 개체가 있어야 존재할수 있는 개체다.

2. 키 개체와 메인 개체의 차이점을 설명하시오.
  - 키 개체는 업무에서 원래부터 독립적으로 존재하는 개체이고, 메인 개체는 키 개체 간의 업무적 관련성 때문에 생성되는 개체이다.

3. 관계 개체와 메인 개체의 차이점을 설명하시오
  - 관계 개체는 두 개체 간의 M:N 관계로 인해 생성되는 개체이고, 메인 개체는 1:1, 1:N, M:N 관련성까지 포함하여 업무의 중심이 되는 개체이다.

4. 다음과 같은 병렬 관계를 직렬 관계로 변환하여 ER 다이어그램으로 표현하시오. (슬라이드 나 책 235쪽)
```text
[부서] M ─── 관리하다 ─── N [공사]
              |
           관리유형
        (발주, 감독, 정산)
```


5. (________) 안에 적합한 단어는?
  > 원래 존재하지는 않았지만 필요에 따라 설계자가 추가한 속성을 (설계)속성이라고한다.

6. 슈퍼 서브 타업을 정제할 때 어떤 경우에 슈퍼타입 개체를 기준으로 통합하는지 설명하시오.
  - 응용 프로그램에서 서브타입을 구분해서 처리하지 않거나, 서브타입 데이터를 명확히 구분하기 어렵거나, 서브타입의 고유 속성 수가 매우 적은 경우 슈퍼타입 개체를 기준으로 통합한다. 

7. 일반회원과 VIP회원을 처리하는 모든응용 프로그램이 병확히 구분되고， 추후 추가될 응용프로그램도 마찬가지다. 아래 슈퍼-서브 타입을 바르게 정제한 결과를 ER 다이어그램으로 표현하시오.
```
          [일반회원]
   ┌──────────────────┐
   │ 번호 (PK)        │
   │ 이름             │
   │ 휴대폰           │
   │ 누적구매액       │
   │ 구매횟수         │
   │ 할인쿠폰수       │
   └──────────────────┘

          [VIP회원]
   ┌──────────────────┐
   │ 번호 (PK)        │
   │ 이름             │
   │ 휴대폰           │
   │ 누적구매액       │
   │ 세부등급         │
   │ 할인율           │
   │ 세일리지         │
   └──────────────────┘
```
8. 사원 가운데 일부는 관리자이므로 유일한 ‘관리자변호’와 ‘직책’， ‘임명일자’， ‘임명기간’ 속성이 필요하여 아래와 같이 사원 개체를 표현했는데， 사원 가운데 관리자의 비율은 5% 미만이다. 이 ER 델을 좀 더 효율적으로 정제해서 ER 다이어그램으로 표현하시오.
```
            [사원]
   ┌──────────────────┐
   │ 사번 (PK)        │
   │ 이름             │
   │ 휴대폰           │
   │ 입사년도         │
   │ 연봉             │
   └──────────────────┘
           │
           │ 1 : 0..1
           │
        ┌──▼──────────────┐
        │    관리자       │
        ├─────────────────┤
        │ 관리자번호      │
        │ 직책            │
        │ 임명일자        │
        │ 임명기간        │
        └─────────────────┘
```
9. 다음과 같이 책을 분류하기 위한 도서 코드code 개체 타입을 ER 다이어그램으로 표현하시오.
  > 도서코드
  > 1: 인문， 2 공학， 3 미술， 4 음악， 5. 취미,...

```
        [도서코드]
   ┌──────────────────┐
   │ 도서코드 (PK)    │
   │ 도서분류명       │
   └──────────────────┘
```

10. 다음 중 속성을 검증하는 방법으로 적절하지 않는 것은?  - 1 -
  1. 각각 의미가 다른 여러 개의 속성을 묶어서 하나의 속성으로 정의한 것이라면 다중치 속성으로 표현해야 한다.
  2. 모든 개체가 통일한 하나의 값만 갖는 속성이 있다면 제거해야 한다.
  3. 코드 값을 갖는 속성이 현업에서 통상적으로 시용되는 코드 값이 아니면 코드 사용을 삼가야 한다.
  4. 전산 처리에 필요한 플래그flag 형태의 속성은 제외해야 한다.

